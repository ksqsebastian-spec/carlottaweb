import HomeContent from "@/components/HomeContent";

export const metadata = {
  title: "Carlotta Pflug — kreieren, organisieren & verbinden in hh.",
};

export default function Page() {
  return <HomeContent lang="de" />;
}
